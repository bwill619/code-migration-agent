# Orchestration
langgraph>=1.2.11
langchain-core>=1.5.5

# Vector database
qdrant-client>=1.19.0

# LLM providers (configure one or both in .env)
openai>=3.1.0
anthropic>=0.122.0

# Environment configuration
python-dotenv>=1.2.2

# Local embeddings fallback (used when OpenAI key is unavailable)
sentence-transformers>=5.7.0

# Validation
pytest>=9.1.1
